package com.ccfraser.muirwik.wrapper.styles

/**
 * ts2kt types with tweaks from material-ui/styles/zIndex
 */
external interface ZIndex {
    var mobileStepper: Int
    var appBar: Int
    var drawer: Int
    var modal: Int
    var snackbar: Int
    var tooltip: Int
}
